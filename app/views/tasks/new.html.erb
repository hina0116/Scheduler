<h1>[ Todo ]</h1>
<%= form_with model: @task, url: '/tasks', method: :post do |f| %>

  <h4>Title</h4>
  <%= f.text_field :name %>

  <h4>Note</h4>
  <%= f.text_area :content %>

  <%= f.submit '投稿' %>
<% end %>